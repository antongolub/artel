---
schema: skill-v1
version: 1
updated_at: 2026-05-03T00:00:00.000Z
description: Integration-level git operations (squash-merge, branch sweeping). Adds `git merge*` to git-write.
tools: Bash(git diff*), Bash(git status*), Bash(git log*), Bash(git show*), Bash(git blame*), Bash(git checkout*), Bash(git add*), Bash(git branch*), Bash(git stash*), Bash(git rebase*), Bash(git merge*)
---

> v1 · updated 2026-05-03

Integration role: bring agent-branch changes onto master's working
tree via squash-merge. Still no `commit` / `push` — owner-only per
AGENTS.md§Branching.
