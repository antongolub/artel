{
  "name": "@antongolub/collab",
  "version": "0.0.0",
  "private": true,
  "description": "Reusable multi-agent coordination platform — role definitions + dispatch engine.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9"
  }
}
